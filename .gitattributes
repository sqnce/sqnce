# Normalize line endings: store as LF, check out as LF on every platform.
# Prevents the Windows/WSL CRLF churn that makes the whole tree show as
# modified and drifts the graphify manifest on each sync.
* text=auto eol=lf
